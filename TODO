TODO:

1. grep for [[legacy]], and debug
2. data format change: node_type & train/test (weight in train/test not used )
3. whole candidate set evaluation (directly incorporated)
4. richer supervised loss (e.g. logistic loss), and evaluation metrics
5. add demo
