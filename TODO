1. data format change: node_type & train/test (weight in train/test not used )
2. whole candidate set evaluation (directly incorporated)
3. richer supervised loss (e.g. logistic loss), and evaluation metrics
4*. grep for legacy, debug, and trick

